# Prompt-Aii Runtime Contract
Prompt-Aii is a first-class runtime planner. It converts natural-language intent into a structured ActionPlan. It does not execute OS actions. NIMO-CORE validates the plan before execution.